/* eslint-disable max-len */

import React from "react";

import { mdiFire } from "@mdi/js";
import Icon from "@mdi/react";

export function FlamesAndFrostSetIndicator() {
  return (
    <Icon path={mdiFire} size={1} />
  );
}
