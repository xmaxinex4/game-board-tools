/* eslint-disable max-len */

import React from "react";

import { mdiLanguagePython } from "@mdi/js";
import Icon from "@mdi/react";

export function KaharianMonsterPackSetIndicator() {
  return (
    <Icon path={mdiLanguagePython} size={1} />
  );
}
