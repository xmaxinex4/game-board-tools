/* eslint-disable max-len */

import React from "react";

import { mdiImageFilterHdr } from "@mdi/js";
import Icon from "@mdi/react";

export function GnollMonsterPackSetIndicator() {
  return (
    <Icon path={mdiImageFilterHdr} size={1} />
  );
}
